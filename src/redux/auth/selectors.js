export const operatorEmailSelector = (state) => state.auth.email
export const submitErrorsSelector = (state) => state.auth.errors
export const tokenSelector = (state) => state.auth.token
export const passwordSelector = (state) => state.auth.password
export const isAuthorizedSelector = (state) => state.auth.successful
